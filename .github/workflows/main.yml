name: Mirror CLI to sigscan-cli

on:
  push:
    branches: [ cli ]
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gstool@cli
        uses: actions/checkout@v4
        with:
          ref: cli
          fetch-depth: 0

      - name: Prepare mirror content
        run: |
          keep=(.git .github cli.py pyproject.toml MANIFEST.in README.md LICENSE analyzers core utils)
          shopt -s extglob dotglob
          for f in * .*; do
            [ "$f" = "." ] || [ "$f" = ".." ] && continue
            [ "$f" = ".git" ] && continue
            case " ${keep[*]} " in
              *" ${f} "*) : ;;   # keep
              *) rm -rf -- "$f" || true ;;
            esac
          done

      - name: Configure SSH
        env:
          DEST_SSH_KEY: ${{ secrets.SIGSCAN_MIRROR_DEPLOY_KEY }}
        run: |
          install -m 700 -d ~/.ssh
          printf "%s" "$DEST_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H github.com >> ~/.ssh/known_hosts

      # Optional: make config global here OR delete this step
      - name: Configure git (global)
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@users.noreply.github.com"

      - name: Push to sigscan-cli@main
        run: |
          git init mirror
          rsync -a --delete --exclude .git ./ mirror/
          cd mirror
          git config user.name "github-actions"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Mirror from gstool@cli: $(date -u +'%Y-%m-%dT%H:%M:%SZ')" || true
          git branch -M main
          git remote add dest git@github.com:zain-sigmoid/sigscan-cli.git
          git push -f dest main
